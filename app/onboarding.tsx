import React from "react";
import { StyleSheet, Text, View } from "react-native";

const onboarding = () => {
  return (
    <View>
      <Text>onboarding bob</Text>
    </View>
  );
};

export default onboarding;

const styles = StyleSheet.create({});
